# Example only. Do not commit real credentials.
# Replace values through your deployment/secret-management workflow.
apiVersion: v1
kind: Secret
metadata:
  name: harbor-registry
  namespace: default
type: kubernetes.io/dockerconfigjson
stringData:
  .dockerconfigjson: |
    {"auths":{"harbor.moein.local":{"username":"REPLACE_ME","password":"REPLACE_ME"}}}
