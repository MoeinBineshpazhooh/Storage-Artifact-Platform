# Example only. Do not commit real credentials.
# Replace values through your deployment/secret-management workflow.
apiVersion: v1
kind: Secret
metadata:
  name: harbor-registry
  namespace: default
type: kubernetes.io/dockerconfigjson
stringData:
  .dockerconfigjson: |
    {"auths":{"registry.example.internal":{"username":"REPLACE_ME","password":"REPLACE_ME"}}}
