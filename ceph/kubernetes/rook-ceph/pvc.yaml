apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rook-ceph-rbd-test
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: rook-ceph-rbd
  resources:
    requests:
      storage: 5Gi
